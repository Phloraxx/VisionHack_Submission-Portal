node_modules/
.react-router/
build/
.env
.env.local
.dev.vars
.slim/deepwork/
.codegraph/
.wrangler/
.playwright-mcp/
.playwright-report/
test-results/
.reasonix/
*.tsbuildinfo

# Logs
*.log

# One-off / ad-hoc scripts (only setup-pb.ts and tests/ are tracked)
scripts/*.mjs
scripts/*.ps1

# AI tooling artifacts
.commandcode/
.impeccable/
.omp/
.tmp/
local/

# Binary / media files — screenshots, test artifacts, binaries
/screenshots/
/test-*.png
/test-idea.pptx
/e2e-final-state.png
/e2e-filled.png
/e2e-register.png
/e2e-result.png
# Root-level PNGs without extension (old screenshot saves)
/after-login
/forgot-password
/forgot-validation
/login-page
/login-validation
/login-with-js

# Docs / review artifacts — not needed for deployment
# AI-generated review artifacts
/app/reviews/
references/
docs/

# CI / config — commit workflow files, ignore the rest
.github/*
!.github/workflows/
!.dockerignore
screens/

# PocketBase hooks — not needed for deployment
pb_hooks/

# Browser tool screenshot artifacts
/*-before.png
/*-after.png
/*-final.png
